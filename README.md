# StudentManagementSystem
Simple backend project developed using Java and Spring framework to manage student records. The application allows users to perform CRUD operations such as adding, updating, deleting, and retrieving student details from DB
